"""Build the reader demo notebooks from reviewable cell sources."""
from pathlib import Path
import nbformat as nbf

ROOT = Path(__file__).resolve().parents[1]
DEMO_DEST = ROOT / 'notebooks' / 'demo'

OVERVIEW_SETUP = '''from IPython.display import display
from onec_runtime.config import RuntimeConfig
from onec_runtime.session import ExtensionMode, RuntimeSessionConfig
from onec_runtime_jupyter import InteractiveRuntimeSession

PLATFORM_BIN = r'C:\\path\\to\\1cv8\\bin'
CONNECTION_STRING = r'File="C:\\path\\to\\ZUP-demo-copy";'
SOURCE_ROOT = r'C:\\path\\to\\ZUP-source'
EXTENSION_MODE = ExtensionMode.AUTO  # Совместимое расширение проверяется при запуске.

runtime = InteractiveRuntimeSession.start(
    RuntimeSessionConfig(
        runtime=RuntimeConfig(
            platform_bin=PLATFORM_BIN,
            connection_string=CONNECTION_STRING,
            # При необходимости задайте username и password локально.
        ),
        source_root=SOURCE_ROOT,
        extension_mode=EXTENSION_MODE,
    )
)'''

OVERVIEW_EMPLOYEES = '''ДатаФОТ = Дата(2021, 8, 1);
ПараметрыСотрудников = КадровыйУчет.ПараметрыПолученияСотрудниковОрганизацийПоСпискуФизическихЛиц();
ПараметрыСотрудников.НачалоПериода = ДатаФОТ;
ПараметрыСотрудников.ОкончаниеПериода = ДатаФОТ;
ПараметрыСотрудников.РаботникиПоТрудовымДоговорам = Истина;

СписокСотрудников = КадровыйУчет.СотрудникиОрганизации(
    Истина, ПараметрыСотрудников).Скопировать(, "Сотрудник");
СписокСотрудников.Свернуть("Сотрудник");
СписокСотрудников.Колонки.Добавить("Период", Новый ОписаниеТипов("Дата"));
Для Каждого СтрокаСотрудника Из СписокСотрудников Цикл
    СтрокаСотрудника.Период = ДатаФОТ;
КонецЦикла;'''

OVERVIEW_PLAN = '''ПлановыйФот = ПлановыеНачисленияСотрудников.ТекущиеДанныеОплатыТрудаСотрудников(
    Неопределено, СписокСотрудников);
ПланФОТ = ПлановыйФот.Скопировать(, "Сотрудник,ФОТ");'''

OVERVIEW_HR_DATA = '''СотрудникиДляДанных = СписокСотрудников.ВыгрузитьКолонку("Сотрудник");
КадровыеДанные = КадровыйУчет.КадровыеДанныеСотрудников(
    Истина,
    СотрудникиДляДанных,
    "Организация,Подразделение,Должность,ФОТ",
    ДатаФОТ
);'''

OVERVIEW_HIRE_SELECT = '''ЗапросПриемов = Новый Запрос;
ЗапросПриемов.Текст =
    "ВЫБРАТЬ
    |   Прием.Ссылка КАК Ссылка,
    |   Прием.Сотрудник КАК Сотрудник,
    |   СтрокаПоказателя.Показатель КАК Показатель,
    |   СтрокаПоказателя.Показатель.Наименование КАК ИмяПоказателя,
    |   СтрокаПоказателя.Значение КАК Значение,
    |   СтрокаПоказателя.ИдентификаторСтрокиВидаРасчета КАК ИдентификаторСтрокиВидаРасчета
    |ИЗ
    |   Документ.ПриемНаРаботу КАК Прием
    |       ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ПриемНаРаботу.Показатели КАК СтрокаПоказателя
    |       ПО Прием.Ссылка = СтрокаПоказателя.Ссылка
    |ГДЕ
    |   Прием.Проведен
    |   И Прием.НачисленияУтверждены
    |   И НЕ Прием.БронированиеПозиции
    |   И Прием.Сотрудник В (&Сотрудники)
    |   И Прием.Дата <= &ДатаДанных
    |УПОРЯДОЧИТЬ ПО
    |   Прием.Дата УБЫВ";
ЗапросПриемов.УстановитьПараметр("ДатаДанных", КонецДня(ДатаФОТ));
ЗапросПриемов.УстановитьПараметр("Сотрудники", СотрудникиДляДанных);
ВыборкаПриемов = ЗапросПриемов.Выполнить().Выбрать();
СсылкаНаПрием = Неопределено;
Пока ВыборкаПриемов.Следующий() Цикл
    Если СтрНайти(НРег(ВыборкаПриемов.ИмяПоказателя), "оклад") = 0 Тогда
        Продолжить;
    КонецЕсли;
    Если ТипЗнч(ВыборкаПриемов.Значение) <> Тип("Число") Тогда
        Продолжить;
    КонецЕсли;
    Если ВыборкаПриемов.Значение <= 0 Тогда
        Продолжить;
    КонецЕсли;
    Кандидат = ВыборкаПриемов.Ссылка.ПолучитьОбъект();
    Для Каждого СтрокаНачисления Из Кандидат.Начисления Цикл
        Если СтрокаНачисления.ИдентификаторСтрокиВидаРасчета = ВыборкаПриемов.ИдентификаторСтрокиВидаРасчета
            И СтрНайти(НРег(Строка(СтрокаНачисления.Начисление)), "оклад") > 0
            И СтрокаНачисления.Размер > 0 Тогда
            СсылкаНаПрием = ВыборкаПриемов.Ссылка;
            СотрудникДляОпыта = ВыборкаПриемов.Сотрудник;
            ПоказательОклада = ВыборкаПриемов.Показатель;
            ИсходныйОклад = ВыборкаПриемов.Значение;
            НачислениеДляОпыта = СтрокаНачисления.Начисление;
            ИсходныйФОТНачисления = СтрокаНачисления.Размер;
            Прием = Кандидат;
            Прием.ДополнительныеСвойства.Вставить(
                "ОтключитьПроверкуДатыЗапретаИзменения", Истина);
            Прервать;
        КонецЕсли;
    КонецЦикла;
    Если СсылкаНаПрием <> Неопределено Тогда
        Прервать;
    КонецЕсли;
КонецЦикла;
Если СсылкаНаПрием = Неопределено Тогда
    ВызватьИсключение "Не найден проведённый прием с показателем и начислением оклада.";
КонецЕсли;
Сообщить(Строка(СсылкаНаПрием) + ": оклад " + Строка(ИсходныйОклад));'''

OVERVIEW_REGISTER_CHECK = '''ЗапросПроверки = Новый Запрос;
ЗапросПроверки.Текст =
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |   Значения.Показатель КАК Показатель,
    |   Значения.Значение КАК Значение
    |ИЗ
    |   РегистрСведений.ЗначенияПериодическихПоказателейРасчетаЗарплатыСотрудников КАК Значения
    |ГДЕ
    |   Значения.Регистратор = &Прием
    |   И Значения.Сотрудник = &Сотрудник
    |   И Значения.Показатель = &Показатель
    |УПОРЯДОЧИТЬ ПО
    |   Значения.Период УБЫВ";
ЗапросПроверки.УстановитьПараметр("Прием", СсылкаНаПрием);
ЗапросПроверки.УстановитьПараметр("Сотрудник", СотрудникДляОпыта);
ЗапросПроверки.УстановитьПараметр("Показатель", ПоказательОклада);
РезультатПроверки = ЗапросПроверки.Выполнить().Выгрузить();'''

OVERVIEW_FOT_CHECK = '''ЗапросНачисления = Новый Запрос;
ЗапросНачисления.Текст =
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |   Начисления.Начисление КАК Начисление,
    |   Начисления.Размер КАК Размер
    |ИЗ
    |   РегистрСведений.ПлановыеНачисления КАК Начисления
    |ГДЕ
    |   Начисления.Регистратор = &Прием
    |   И Начисления.Сотрудник = &Сотрудник
    |   И Начисления.Начисление = &Начисление
    |УПОРЯДОЧИТЬ ПО
    |   Начисления.Период УБЫВ";
ЗапросНачисления.УстановитьПараметр("Прием", СсылкаНаПрием);
ЗапросНачисления.УстановитьПараметр("Сотрудник", СотрудникДляОпыта);
ЗапросНачисления.УстановитьПараметр("Начисление", НачислениеДляОпыта);
РезультатНачисления = ЗапросНачисления.Выполнить().Выгрузить();

ЗапросФОТДокумента = Новый Запрос;
ЗапросФОТДокумента.Текст =
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |   ПлановыйФОТ.Период КАК Период,
    |   ПлановыйФОТ.ФОТ КАК ФОТ
    |ИЗ
    |   РегистрСведений.ПлановыйФОТИтоги КАК ПлановыйФОТ
    |ГДЕ
    |   ПлановыйФОТ.РегистраторСобытия = &Прием
    |   И ПлановыйФОТ.Сотрудник = &Сотрудник
    |УПОРЯДОЧИТЬ ПО
    |   ПлановыйФОТ.Период УБЫВ";
ЗапросФОТДокумента.УстановитьПараметр("Прием", СсылкаНаПрием);
ЗапросФОТДокумента.УстановитьПараметр("Сотрудник", СотрудникДляОпыта);
РезультатФОТДокумента = ЗапросФОТДокумента.Выполнить().Выгрузить();'''

CAPTURE_ARM = '''MODULE_PATH = r'CommonModules\\КадровыйУчет\\Ext\\Module.bsl'
runtime.add_capture_point(MODULE_PATH, 127)  # A: выгрузка результата запроса
runtime.add_capture_point(MODULE_PATH, 129)  # B: возврат таблицы'''

CAPTURE_CALL = '''ПланПовтор = КадровыйУчет.КадровыеДанныеСотрудников(
    Истина, СотрудникиДемо,
    "ФОТ,Подразделение,ГоловнаяОрганизация,Организация", ДатаФОТ);
Результат = ПланПовтор.Итог("ФОТ");'''

CAPTURE_READ = '''ЗапросДемоВТ = Новый Запрос;
ЗапросДемоВТ.МенеджерВременныхТаблиц = КонтекстОтладки.Запрос.МенеджерВременныхТаблиц;
ЗапросДемоВТ.Текст = "ВЫБРАТЬ Сотрудник, Организация, ФОТ ИЗ ВТКадровыеДанныеСотрудников";
СнимокВТ = ЗапросДемоВТ.Выполнить().Выгрузить();'''

CAPTURE_OUTPUT = '''СнимокВыхода = КонтекстОтладки.КадровыеДанныеСотрудников.Скопировать(
    , "Сотрудник,Организация,ФОТ");'''

OVERVIEW_MATERIALIZE = '''# КадровыеДанные пока живёт в 1С; to_df() переносит таблицу в pandas.
df = КадровыеДанные.to_df(refs="presentation")
display(df.head(10))
print("Строк с кадровыми данными:", len(df))'''

OVERVIEW_PLAN_CHART = '''import matplotlib.pyplot as plt

by_department = (
    df.groupby("Подразделение", dropna=False)["ФОТ"]
      .sum()
      .sort_values()
)
display(by_department.to_frame("ФОТ"))
fig, ax = plt.subplots(figsize=(9, max(4, len(by_department) * 0.38)))
by_department.map(float).plot.barh(ax=ax)
ax.set_xlabel("ФОТ, ₽")
ax.set_title("Плановый ФОТ по подразделениям на 01.08.2021")
fig.tight_layout()
display(fig)
plt.close(fig)'''

OVERVIEW_CAPTURE_ARM = '''from pathlib import Path

CAPTURE_MODULE_PATH = r'CommonModules\\РасчетЗарплатыРасширенный\\Ext\\Module.bsl'
capture_source = (Path(SOURCE_ROOT) / CAPTURE_MODULE_PATH).read_text(encoding="utf-8-sig")
capture_marker = 'Если СтруктураДанных.Свойство("ДанныеОПлановыхНачислениях") Тогда'
capture_lines = [
    number for number, line in enumerate(capture_source.splitlines(), start=1)
    if line.strip() == capture_marker
]
assert len(capture_lines) == 1
runtime.add_capture_point(CAPTURE_MODULE_PATH, capture_lines[0])'''

OVERVIEW_CAPTURE_MUTATE = '''СтрокиОклада = КонтекстОтладки.СтруктураДанных.ЗначенияПоказателей.НайтиСтроки(
    Новый Структура("Сотрудник,Показатель", СотрудникДляОпыта, ПоказательОклада)
);
Если СтрокиОклада.Количество() <> 1 Тогда
    ВызватьИсключение "Ожидалась одна строка показателя оклада.";
КонецЕсли;
СтрокаОклада = СтрокиОклада[0];
Если СтрокаОклада.Значение <> ИсходныйОклад Тогда
    ВызватьИсключение "Оклад изменился после выбора документа.";
КонецЕсли;

СтрокиФОТ = КонтекстОтладки.СтруктураДанных.ДанныеОПлановыхНачислениях.НайтиСтроки(
    Новый Структура("Сотрудник,Начисление", СотрудникДляОпыта, НачислениеДляОпыта)
);
Если СтрокиФОТ.Количество() <> 1 Тогда
    ВызватьИсключение "Ожидалась одна строка планового начисления.";
КонецЕсли;
СтрокаФОТ = СтрокиФОТ[0];
Если СтрокаФОТ.Размер <> ИсходныйФОТНачисления Тогда
    ВызватьИсключение "Плановый ФОТ изменился после выбора документа.";
КонецЕсли;
СтрокаОклада.Значение = УвеличитьНаПроцент(СтрокаОклада.Значение);
СтрокаФОТ.Размер = УвеличитьНаПроцент(СтрокаФОТ.Размер);
Сообщить("Оклад внутри проведения: " + Строка(СтрокаОклада.Значение));'''

OVERVIEW_RELOADED_METHOD = '''Функция ТекущиеДанныеОплатыТрудаСотрудников(Ссылка, СотрудникиДаты) Экспорт
    ПараметрыПостроения = ЗарплатаКадрыОбщиеНаборыДанных.ПараметрыПостроенияДляСоздатьВТИмяРегистраСрез();
    ПараметрыПостроения.ФормироватьСПериодичностьДень = Ложь;
    ЗарплатаКадрыОбщиеНаборыДанных.ДобавитьВКоллекциюОтбор(
        ПараметрыПостроения.Отборы, "Регистратор", "<>", Ссылка);
    Запрос = Новый Запрос;
    Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
    ЗарплатаКадрыОбщиеНаборыДанных.СоздатьВТИмяРегистраСрезПоследних(
        "ПлановыйФОТИтоги",
        Запрос.МенеджерВременныхТаблиц,
        Истина,
        ЗарплатаКадрыОбщиеНаборыДанных.ОписаниеФильтраДляСоздатьВТИмяРегистра(
            СотрудникиДаты, "Сотрудник"),
        ПараметрыПостроения);
    Запрос.Текст =
        "ВЫБРАТЬ
        |   ЗначенияСовокупныхТарифныхСтавок.Сотрудник,
        |   ЗначенияСовокупныхТарифныхСтавок.Период,
        |   ЗначенияСовокупныхТарифныхСтавок.СовокупнаяТарифнаяСтавка КАК СовокупнаяТарифнаяСтавка,
        |   ЗначенияСовокупныхТарифныхСтавок.ВидТарифнойСтавки,
        |   ЗначенияСовокупныхТарифныхСтавок.ФОТ КАК ФОТ,
        |   ЗначенияСовокупныхТарифныхСтавок.ФОТ * 1.30 КАК ФОТСоСтраховыми
        |ИЗ
        |   ВТПлановыйФОТИтогиСрезПоследних КАК ЗначенияСовокупныхТарифныхСтавок";
    ЗначенияДанныхОплатыТруда = Запрос.Выполнить().Выгрузить();
    Возврат ЗначенияДанныхОплатыТруда;
КонецФункции'''

UT_SETUP = '''from IPython.display import display
from onec_runtime.config import RuntimeConfig
from onec_runtime.session import ExtensionMode, RuntimeSessionConfig
from onec_runtime_jupyter import InteractiveRuntimeSession

PLATFORM_BIN = r'C:\\path\\to\\1cv8\\bin'
CONNECTION_STRING = r'File="C:\\demo\\UT";'
SOURCE_ROOT = r'C:\\exports\\UT'
EXTENSION_MODE = ExtensionMode.AUTO  # Совместимое расширение проверяется при запуске.

runtime = InteractiveRuntimeSession.start(
    RuntimeSessionConfig(
        runtime=RuntimeConfig(
            platform_bin=PLATFORM_BIN,
            connection_string=CONNECTION_STRING,
        ),
        source_root=SOURCE_ROOT,
        extension_mode=EXTENSION_MODE,
    )
)'''

UT_LAST_SALE = '''ЗапросПоследнейПродажи = Новый Запрос;
ЗапросПоследнейПродажи.Текст =
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |   Движения.Период КАК Период
    |ИЗ
    |   РегистрНакопления.ВыручкаИСебестоимостьПродаж КАК Движения
    |ГДЕ
    |   Движения.Активность
    |   И Движения.СуммаВыручки <> 0
    |УПОРЯДОЧИТЬ ПО
    |   Движения.Период УБЫВ";
ПоследняяПродажа = ЗапросПоследнейПродажи.Выполнить().Выбрать();
Если Не ПоследняяПродажа.Следующий() Тогда
    ВызватьИсключение "В регистре нет активных движений выручки. Выберите ИБ с продажами.";
КонецЕсли;

НачалоПоследнегоМесяца = НачалоМесяца(ПоследняяПродажа.Период);
НачалоПредыдущегоМесяца = ДобавитьМесяц(НачалоПоследнегоМесяца, -1);
КонецПоследнегоМесяца = КонецМесяца(ПоследняяПродажа.Период);'''

UT_SALES = '''ЗапросПродаж = Новый Запрос;
ЗапросПродаж.Текст =
    "ВЫБРАТЬ
    |   Продажи.Период КАК Месяц,
    |   Продажи.Подразделение КАК Подразделение,
    |   СУММА(Продажи.СуммаВыручкиОборот) КАК Выручка
    |ИЗ
    |   РегистрНакопления.ВыручкаИСебестоимостьПродаж.Обороты(
    |       &НачалоПериода, &КонецПериода, Месяц, ) КАК Продажи
    |СГРУППИРОВАТЬ ПО
    |   Продажи.Период,
    |   Продажи.Подразделение
    |УПОРЯДОЧИТЬ ПО
    |   Месяц";
ЗапросПродаж.УстановитьПараметр("НачалоПериода", НачалоПредыдущегоМесяца);
ЗапросПродаж.УстановитьПараметр("КонецПериода", КонецПоследнегоМесяца);
ПродажиПоМесяцам = ЗапросПродаж.Выполнить().Выгрузить();
Если ПродажиПоМесяцам.Количество() = 0 Тогда
    ВызватьИсключение "За выбранные два месяца оборотов выручки нет.";
КонецЕсли;'''

UT_TOTALS = '''ВыручкаПредыдущегоМесяца = 0;
ВыручкаПоследнегоМесяца = 0;
Для Каждого СтрокаПродаж Из ПродажиПоМесяцам Цикл
    Если СтрокаПродаж.Месяц = НачалоПоследнегоМесяца Тогда
        ВыручкаПоследнегоМесяца = ВыручкаПоследнегоМесяца + СтрокаПродаж.Выручка;
    Иначе
        ВыручкаПредыдущегоМесяца = ВыручкаПредыдущегоМесяца + СтрокаПродаж.Выручка;
    КонецЕсли;
КонецЦикла;

ПриростВыручки = ПродажиСервер.ПроцентПрироста(
    ВыручкаПредыдущегоМесяца, ВыручкаПоследнегоМесяца);
КонтрольныйПриростДо = ПродажиСервер.ПроцентПрироста(0, 100);'''

UT_CAPTURE_ARM = '''runtime.add_capture_point(r'CommonModules\\ПродажиСервер\\Ext\\Module.bsl', 9311)'''

UT_CAPTURE_READ = '''СнимокПрироста = Новый ТаблицаЗначений;
СнимокПрироста.Колонки.Добавить("ВыручкаПредыдущегоМесяца");
СнимокПрироста.Колонки.Добавить("ВыручкаПоследнегоМесяца");
СнимокПрироста.Колонки.Добавить("ПриростПроцентов");
СтрокаСнимка = СнимокПрироста.Добавить();
СтрокаСнимка.ВыручкаПредыдущегоМесяца = КонтекстОтладки.ПредыдущееЗначение;
СтрокаСнимка.ВыручкаПоследнегоМесяца = КонтекстОтладки.ТекущееЗначение;
СтрокаСнимка.ПриростПроцентов = КонтекстОтладки.Результат;'''

UT_RELOADED_BRANCH = '''ИначеЕсли ПредыдущееЗначение = 0 Тогда
    Результат = Неопределено;'''

def md(text): return nbf.v4.new_markdown_cell(text)
def py(source): return nbf.v4.new_code_cell(source)
def bsl(source): return py('%%bsl\n' + source)

def opening(title, goal, *, setup=OVERVIEW_SETUP, setup_note='', snapshot_note='ЗУП КОРП 3.1.38.92, платформа 8.5.1.1529, дата данных 01.08.2021. '):
    return [md('# '+title+'\n\n'+goal+'\n\n'+snapshot_note+
        'Используйте отдельную демо-копию и соответствующую ей выгрузку исходников. '
        'Подготовка окружения — в [README](../README.md). Сеанс закрывается последней ячейкой; при прерывании выполните `runtime.close()`.'),
        md('## Подготовка' + setup_note), py(setup)]

def overview_reload_cells():
    return [
        md('## Hot reload: один короткий опыт\n\nВ **локальной копии** выгрузки '
           'откройте `CommonModules/ПлановыеНачисленияСотрудников/Ext/Module.bsl`. '
           'В запросе метода `ТекущиеДанныеОплатыТрудаСотрудников` добавьте '
           'поле `ЗначенияСовокупныхТарифныхСтавок.ФОТ * 1.30 КАК ФОТСоСтраховыми` '
           'и сохраните файл. Исходный `ФОТ` остаётся в результате; новое поле '
           'показывает условную надбавку 30%. Конфигурация ИБ не изменяется.\n\n'
           '<details><summary>Полная версия метода для локальной замены</summary>\n\n'
           '```bsl\n' + OVERVIEW_RELOADED_METHOD + '\n```\n\n</details>\n\n'
           'Загрузим изменённый метод в текущий сеанс и повторим прежний вызов.'),
        py("RELOAD_MODULE_PATH = r'CommonModules\\ПлановыеНачисленияСотрудников\\Ext\\Module.bsl'\n"
           "runtime.load_worker_module(RELOAD_MODULE_PATH)"),
        bsl('ПлановыйФотСоСтраховыми = ПлановыеНачисленияСотрудников.ТекущиеДанныеОплатыТрудаСотрудников(\n'
            '    Неопределено, СписокСотрудников);\n'
            'ПланСоСтраховыми = ПлановыйФотСоСтраховыми.Скопировать(, '
            '"Сотрудник,ФОТ,ФОТСоСтраховыми");'),
        py('reloaded = ПланСоСтраховыми.to_df(refs="presentation")\n'
           'display(reloaded.head(10))\n'
           'print("ФОТ:", reloaded["ФОТ"].sum(), "₽")\n'
           'print("ФОТ со страховыми:", reloaded["ФОТСоСтраховыми"].sum(), "₽")'),
        md('К изменённому коду типового метода вернёмся во второй статье. '
           'Теперь остановим проведение документа.'),
    ]

def capture_control():
    return [
        md('## Две точки внутри типового метода\n\nA — перед выгрузкой итогового запроса '
           '(строка 127); B — перед возвратом таблицы (строка 129). '
           'Номера относятся к выгрузке ЗУП КОРП 3.1.38.92. '
           'Точки задаём путём к модулю и номером строки.'),
        py(CAPTURE_ARM),
        md('## Запускаем вызов\n\nВызов находится в отдельной BSL-ячейке. '
           'Она остановится в точке A; последующие ячейки работают с тем же вызовом.'),
        bsl(CAPTURE_CALL),
        py('''import pandas as pd

status_a = runtime.status()
assert status_a.state.value == 'captured'
stack = runtime.runtime_api.capture_stack(cursor=0, limit=20)
display(pd.DataFrame(stack['frames'])[['level', 'module_type', 'line']])
print('Кадров в стеке:', stack['total'])'''),
        md('## Читаем временную таблицу\n\n`КонтекстОтладки` доступен, пока исходный вызов остановлен. '
           'Переносим в Python только три нужных столбца.'),
        bsl(CAPTURE_READ),
        py('''temporary = СнимокВТ.to_df(refs='uuid')
display(temporary)
print('Строк:', len(temporary), 'ФОТ:', temporary['ФОТ'].sum(), '₽')'''),
        py('''resume_b = runtime.resume_capture()
assert resume_b.state.value == 'captured'
assert resume_b.operation_id == status_a.operation_id
print('Останов B:', resume_b.stop_sequence)'''),
        bsl(CAPTURE_OUTPUT),
        py('''captured_output = СнимокВыхода.to_df(refs='uuid')
keys = ['Сотрудник', 'Организация']
sort_rows = lambda frame: frame.sort_values(keys).reset_index(drop=True)
pd.testing.assert_frame_equal(sort_rows(temporary), sort_rows(captured_output))
display(captured_output)'''),
        py('''completed = runtime.resume_capture()
assert completed.succeeded and completed.state.value == 'completed'
assert completed.operation_id == resume_b.operation_id
runtime.clear_capture_points()
print('Обычный результат:', completed.result, '₽')'''),
    ]

def overview_capture_cells():
    return [
        md('## Capture внутри проведения «Приема на работу»\n\n'
           'Выбранный документ уже проведён в демокопии. Повторное проведение '
           'из notebook остановим в `РасчетЗарплатыРасширенный` перед обработкой '
           'таблиц плановых начислений и значений показателей. '
           'Номер строки определяем по локальной выгрузке ЗУП КОРП 3.1.38.92: '
           'он может отличаться между форматами выгрузки. '
           'Для финального опыта возвращаем повышение к 10%.'),
        bsl('ПроцентПовышения = 10;'),
        py(OVERVIEW_CAPTURE_ARM),
        bsl('Прием.Записать(РежимЗаписиДокумента.Проведение);'),
        py("capture_status = runtime.status()\n"
           "assert capture_status.state.value == 'captured'\n"
           "print('Проведение остановлено:', capture_status.operation_id)"),
        md('Выполнение документа ещё не завершилось. '
           'Через `КонтекстОтладки` видны таблицы именно этого вызова. '
           'Для просмотра копируем только нужные столбцы.'),
        bsl('СнимокПоказателей = КонтекстОтладки.СтруктураДанных.ЗначенияПоказателей.Скопировать(\n'
            '    , "Сотрудник,Показатель,Значение");\n'
            'СнимокПлановыхНачислений = КонтекстОтладки.СтруктураДанных.ДанныеОПлановыхНачислениях.Скопировать(\n'
            '    , "Сотрудник,Начисление,Размер");'),
        py('display(СнимокПоказателей.head(10).to_df(refs="presentation"))\n'
           'display(СнимокПлановыхНачислений.head(10).to_df(refs="presentation"))'),
        md('Теперь вызываем ту же функцию `УвеличитьНаПроцент`, которую объявили '
           'в начале notebook. Меняем значение показателя оклада и размер '
           'соответствующего планового начисления в живых данных проведения.'),
        bsl(OVERVIEW_CAPTURE_MUTATE),
        py("completed = runtime.resume_capture()\n"
           "runtime.clear_capture_points()\n"
           "assert completed.succeeded and completed.state.value == 'completed'\n"
           "assert completed.operation_id == capture_status.operation_id\n"
           "print('Продолжилось и завершилось то же проведение')"),
        md('## Проверяем запись и плановый ФОТ\n\nСначала прочитаем движения '
           'именно этого документа: значение периодического показателя и размер '
           'планового начисления. Отдельно посмотрим запись регистра '
           '`ПлановыйФОТИтоги`, связанную с тем же приемом.'),
        bsl(OVERVIEW_REGISTER_CHECK),
        py('''from decimal import Decimal

indicator = РезультатПроверки.to_df(refs="presentation")
assert len(indicator) == 1
before_salary = ИсходныйОклад.materialize()
expected_salary = before_salary * Decimal("1.10")
assert indicator.iloc[0]["Значение"] == expected_salary
display(indicator)
print("Показатель оклада:", before_salary, "→", expected_salary)'''),
        bsl(OVERVIEW_FOT_CHECK),
        py('''accrual = РезультатНачисления.to_df(refs="presentation")
expected_accrual = ИсходныйФОТНачисления.materialize() * Decimal("1.10")
assert len(accrual) == 1
assert accrual.iloc[0]["Размер"] == expected_accrual
display(accrual)
print("Плановое начисление:", ИсходныйФОТНачисления.materialize(), "→", expected_accrual)

fot_record = РезультатФОТДокумента.to_df(refs="presentation")
display(fot_record)
print("Плановый ФОТ по документу:",
      fot_record.iloc[0]["ФОТ"] if len(fot_record) else "нет записи")'''),
        md('Типовой метод показывает плановый ФОТ на 01.08.2021. Если после '
           'приема были другие кадровые события, этот срез может не измениться '
           'от повторного проведения раннего документа.'),
        bsl('ПланПослеПроведения = ПлановыеНачисленияСотрудников.ТекущиеДанныеОплатыТрудаСотрудников(\n'
            '    Неопределено, СписокСотрудников);\n'
            'ПланПослеКратко = ПланПослеПроведения.Скопировать(, "Сотрудник,ФОТ");'),
        py('''employee_uuid = СотрудникДляОпыта.materialize().uuid
before_plan = ПланФОТ.to_df(refs="uuid")
after_plan = ПланПослеКратко.to_df(refs="uuid")
before_value = before_plan.loc[before_plan["Сотрудник"] == employee_uuid, "ФОТ"]
after_value = after_plan.loc[after_plan["Сотрудник"] == employee_uuid, "ФОТ"]
if len(before_value) and len(after_value):
    print("Плановый ФОТ на дату снимка:", before_value.iloc[0], "→", after_value.iloc[0])
else:
    print("В срезе на дату снимка нет строки этого сотрудника; ФОТ документа показан выше.")'''),
    ]

def cleanup(): return [md('## Завершение'),py("runtime.close()\nprint('Сеанс закрыт')")]

def ut_reload_cells():
    return [
        md('## Hot reload: нет базы для процента\n\nТиповая функция возвращает 100% '
           'для пары `(0, 100)`. Попробуем другую трактовку: при нулевой '
           'предыдущей и положительной текущей выручке процент не определён. '
           'В **локальной копии** выгрузки `SOURCE_ROOT` откройте '
           '`CommonModules/ПродажиСервер/Ext/Module.bsl`. В функции '
           '`ПроцентПрироста` найдите ветку `ИначеЕсли ПредыдущееЗначение = 0 Тогда` '
           'и замените в ней `Результат = 100;` на `Результат = Неопределено;`. '
           'После правки ветка должна выглядеть так:\n\n'
           '```bsl\n' + UT_RELOADED_BRANCH + '\n```\n\n'
           'Сохраните файл, оставив остальную функцию без изменений. '
           'Конфигурация ИБ не меняется: следующий шаг загрузит изменённый '
           'модуль в текущий runtime-сеанс. При переносе в Python значение '
           '`Неопределено` представлено как `ONEC_UNDEFINED`.'),
        py("runtime.load_worker_module(r'CommonModules\\ПродажиСервер\\Ext\\Module.bsl')"),
        bsl('ПриростПослеПерезагрузки = ПродажиСервер.ПроцентПрироста(\n'
            '    ВыручкаПредыдущегоМесяца, ВыручкаПоследнегоМесяца);\n'
            'КонтрольныйПриростПосле = ПродажиСервер.ПроцентПрироста(0, 100);'),
        py('''from onec_runtime.value_materialization import ONEC_UNDEFINED

growth_after = ПриростПослеПерезагрузки.materialize()
probe_after = КонтрольныйПриростПосле.materialize()
assert probe_before == 100 and probe_after is ONEC_UNDEFINED
print('Контрольный вызов (0, 100):', probe_before, '→ Неопределено')
print('Выручка ИБ:', growth_before, '→',
      'Неопределено' if growth_after is ONEC_UNDEFINED else growth_after)'''),
    ]

def ut_overview_cells():
    cells = opening(
        'Продажи УТ: выручка, capture и hot reload',
        'Найдём последний месяц с выручкой и предыдущий календарный месяц, сравним обороты '
        'по подразделениям, остановим расчёт прироста и изменим его через hot reload.',
        setup=UT_SETUP,
        snapshot_note='Метаданные сверены с УТ 11.6.1.61; дата данных ИБ не фиксируется. ',
        setup_note='\n\nУкажите `PLATFORM_BIN`, `CONNECTION_STRING` и `SOURCE_ROOT` '
                   'для отдельной копии УТ 11.6.1.61. В `SOURCE_ROOT` нужна выгрузка '
                   'исходников этой же конфигурации. Если в ИБ есть пользователи, '
                   'задайте `username` и `password` в `RuntimeConfig` локально.',
    )
    cells += [
        md('## От какого месяца считать\n\nБерём последнюю дату активного движения '
           'с ненулевой выручкой. Сравниваем месяц этой даты с предыдущим '
           'календарным месяцем. Если движений нет, ячейка остановится '
           'с понятной ошибкой вместо пустой диаграммы.'),
        bsl(UT_LAST_SALE),
        md('## Обороты регистра\n\nВиртуальная таблица `Обороты` '
           'учитывает движения выручки, в том числе корректировки и возвраты. '
           'Группируем по месяцу и подразделению; `СуммаВыручки` — ресурс '
           'регистра с НДС. В этой выборке нет пересчёта сумм документов по валютам.'),
        bsl(UT_SALES),
        md('## Таблица и диаграммы\n\nПереносим результат `%%bsl` в pandas. '
           'Подписи ссылок получаем через `refs="presentation"`; '
           'они нужны только для показа, а не для сопоставления объектов. '
           'Месяц без движений показываем с нулевой выручкой.'),
        py('''import pandas as pd
import matplotlib.pyplot as plt

sales = ПродажиПоМесяцам.to_df(refs='presentation')
display(sales)
monthly = (sales.assign(Месяц=pd.to_datetime(sales['Месяц']).dt.to_period('M'))
           .groupby('Месяц')['Выручка'].sum())
months = pd.period_range(end=monthly.index.max(), periods=2, freq='M')
monthly = monthly.reindex(months, fill_value=0)
display(monthly.rename_axis('Месяц').to_frame())

fig, ax = plt.subplots(figsize=(7, 4))
ax.bar([str(month) for month in monthly.index], monthly.map(float))
ax.set_ylabel('Выручка с НДС')
ax.set_title('Два последних календарных месяца по данным ИБ')
fig.tight_layout()
display(fig)
plt.close(fig)'''),
        py('''latest_month = sales['Месяц'].max()
by_department = (sales.loc[sales['Месяц'] == latest_month]
                 .assign(Подразделение=lambda data:
                         data['Подразделение'].fillna('Без подразделения'))
                 .groupby('Подразделение')['Выручка'].sum()
                 .sort_values(ascending=False).head(10))
display(by_department.to_frame('Выручка'))

fig, ax = plt.subplots(figsize=(9, max(3, len(by_department) * 0.4)))
ax.barh(by_department.index, by_department.map(float))
ax.invert_yaxis()
ax.set_xlabel('Выручка с НДС')
ax.set_title(f'Подразделения за {str(latest_month)[:7]}')
fig.tight_layout()
display(fig)
plt.close(fig)'''),
        md('## Типовая логика прироста\n\nСначала суммируем выручку '
           'в BSL и вызываем `ПродажиСервер.ПроцентПрироста`. '
           'В УТ 11.6.1.61 эта функция отдельно обрабатывает нулевую '
           'и отрицательную базу сравнения: результат для нулевой базы '
           'не равен обычному математическому проценту роста.'),
        bsl(UT_TOTALS),
        py("growth_before = ПриростВыручки.materialize()\n"
           "probe_before = КонтрольныйПриростДо.materialize()\n"
           "print('Прирост выручки, %:', growth_before)\n"
           "print('Контрольный вызов (0, 100), %:', probe_before)"),
        md('## Останов перед возвратом\n\nПоставим точку на строке 9311 '
           'метода `ПродажиСервер.ПроцентПрироста`, перед `Возврат Результат;`. '
           'Номер относится к выгрузке УТ 11.6.1.61.'),
        py(UT_CAPTURE_ARM),
        bsl('Результат = ПродажиСервер.ПроцентПрироста(\n'
            '    ВыручкаПредыдущегоМесяца, ВыручкаПоследнегоМесяца);'),
        py('''status = runtime.status()
assert status.state.value == 'captured'
stack = runtime.runtime_api.capture_stack(cursor=0, limit=20)
display(pd.DataFrame(stack['frames'])[['level', 'module_type', 'line']])'''),
        md('## Значения внутри вызова\n\nПока вызов остановлен, '
           '`КонтекстОтладки` даёт аргументы и локальный результат. '
           'Скопируем только три числа в таблицу для просмотра в Python.'),
        bsl(UT_CAPTURE_READ),
        py("display(СнимокПрироста.to_df())"),
        py('''completed = runtime.resume_capture()
runtime.clear_capture_points()
assert completed.succeeded and completed.state.value == 'completed'
print('Результат после продолжения, %:', completed.result)'''),
    ]
    cells += ut_reload_cells()
    cells += [
        md('## Что дальше\n\nМожно заменить два месяца другим интервалом, '
           'добавить отбор по организации или исследовать отдельное '
           'подразделение. Для сравнения с ЗУП откройте '
           '[01-overview.ipynb](../ZUP/01-overview.ipynb).'),
    ]
    return cells + cleanup()

def build():
    overview = opening('BSL в Jupyter: от «Привет, мир!» до проведения ЗУП',
        'Начнём с нескольких BSL-ячеек, затем возьмём данные из настоящей ЗУП, '
        'исследуем их в Python и остановим проведение документа.',
        setup=OVERVIEW_SETUP,
        snapshot_note='Конфигурация: ЗУП КОРП 3.1.38.92; дата снимка данных: 01.08.2021. ',
        setup_note='\n\nУкажите `PLATFORM_BIN`, `CONNECTION_STRING` и `SOURCE_ROOT` '
                   'для отдельной копии ЗУП и выгрузки её исходников. '
                   'Если нужны учётные данные, задайте их локально в `RuntimeConfig`. '
                   'Эта служебная ячейка один раз открывает сеанс; далее идёт BSL.')
    overview += [
        md('## Первая BSL-ячейка\n\n`%%bsl` выполняет содержимое ячейки как BSL '
           'в настоящем сеансе 1С. Сообщение появляется прямо под ячейкой.'),
        bsl('Сообщить("Привет, мир!");'),
        md('## Состояние между ячейками\n\nСохраним процент повышения. '
           'Следующая BSL-ячейка увидит эту переменную.'),
        bsl('ПроцентПовышения = 10;'),
        bsl('Сообщить(ПроцентПовышения);'),
        md('## Метод прямо в notebook\n\nФункция тоже остаётся доступной '
           'в следующих ячейках.'),
        bsl('Функция УвеличитьНаПроцент(Значение)\n'
            '    Возврат Значение * (1 + ПроцентПовышения / 100);\n'
            'КонецФункции'),
        bsl('Сообщить(УвеличитьНаПроцент(100000));'),
        bsl('ПроцентПовышения = 20;'),
        bsl('Сообщить(УвеличитьНаПроцент(100000));'),
        md('Первый вызов выводит 110000, второй — 120000. '
           'Функция объявлена раньше, но при повторном вызове читает '
           'текущее значение `ПроцентПовышения` из notebook-контекста. '
           'Это похоже на работу с внешней переменной функции в Python/Jupyter.'),
        md('## Та же сессия — внутри ЗУП\n\nТеперь используем типовой '
           '`КадровыйУчет.СотрудникиОрганизации`. Дата 01.08.2021 относится '
           'к данным демоснимка, а 3.1.38.92 — к версии конфигурации. '
           'Список сотрудников затем пригодится и для расчёта ФОТ.'),
        bsl(OVERVIEW_EMPLOYEES),
        md('Другой типовой метод возвращает кадровые данные и ФОТ '
           'для этих сотрудников на ту же дату.'),
        bsl(OVERVIEW_HR_DATA),
        md('## Из 1С в Python\n\n`КадровыеДанные` пока живёт в 1С; '
           'одноимённая Python-переменная — proxy. `to_df()` материализует '
           'таблицу в обычный `pandas.DataFrame`.'),
        py(OVERVIEW_MATERIALIZE),
        md('Для ссылок можно выбрать представление, UUID или оба значения. '
           'Представление удобно читать, UUID — использовать как идентификатор '
           'при сравнении и соединении таблиц.'),
        py('''df_uuid = КадровыеДанные.to_df(refs="uuid")
df_both = КадровыеДанные.to_df(refs="both")
display(df_uuid[["Сотрудник", "Подразделение"]].head())
display(df_both[["Сотрудник", "Сотрудник__uuid", "Подразделение"]].head())'''),
        md('## Обычный pandas\n\nСгруппируем ФОТ по подразделению и '
           'построим одну диаграмму. Бизнес-данные получила ЗУП; '
           'Python нужен для исследования результата.'),
        py(OVERVIEW_PLAN_CHART),
        md('## Материализуем объект документа\n\nВозьмём проведённый '
           '`ПриемНаРаботу` с показателем и начислением оклада у сотрудника '
           'из выбранного списка. Служебная ячейка найдёт подходящий документ '
           'в демокопии и запомнит исходные значения для финального опыта.'),
        bsl(OVERVIEW_HIRE_SELECT),
        bsl('ПоказателиПриема = Прием.Показатели.Выгрузить();\n'
            'НачисленияПриема = Прием.Начисления.Выгрузить();'),
        py('''data = Прием.materialize(refs="presentation")
print(data["Дата"], data["Организация"], data["Сотрудник"])
print("Табличные части:", data.tabular_sections)
display(ПоказателиПриема.head(5).to_df(refs="presentation"))
display(НачисленияПриема.head(5).to_df(refs="presentation"))'''),
        md('`materialize()` работает не только с таблицами: объект документа '
           'даёт Python-снимок реквизитов и список табличных частей. '
           'Нужную табличную часть можно выгрузить отдельно. Аналогично '
           'материализуются структуры, массивы и соответствия.'),
        md('## Большую таблицу можно читать порциями\n\nЕсли нужен быстрый '
           'просмотр, ограничиваем перенос на стороне 1С.'),
        py('display(КадровыеДанные.head(10).to_df(refs="presentation"))'),
        md('## Notebook в VS Code\n\nДля `%%bsl` доступны подсветка, '
           'автодополнение, сигнатуры, hover и переход к определению. '
           'Например, из вызова `КадровыйУчет.КадровыеДанныеСотрудников` '
           'можно перейти к методу в выгрузке ЗУП. '
           'Для статьи здесь уместен скриншот этой ячейки с открытым определением.'),
        md('## Зафиксируем исходный плановый ФОТ\n\nТиповой метод '
           '`ТекущиеДанныеОплатыТрудаСотрудников` вернёт ФОТ до экспериментов. '
           'Этот результат пригодится для сравнения после проведения.'),
        bsl(OVERVIEW_PLAN),
        py('display(ПланФОТ.head(10).to_df(refs="presentation"))'),
    ]
    overview += overview_reload_cells() + overview_capture_cells()
    overview += [md('## От первой ячейки до живого проведения\n\nНачали с '
                   '`Сообщить("Привет, мир!")`, а закончили изменением данных '
                   'внутри остановленного типового проведения и продолжением '
                   'того же вызова. Во второй статье можно подробно разобрать '
                   'hot reload, в третьей — capture. '
                   'Расширенный опыт с двумя точками останова, стеком и временной '
                   'таблицей — в [03-capture.ipynb](03-capture.ipynb).'),
                 md('## Завершение'), py('runtime.close()')]

    capture = opening('Capture: временная таблица внутри типового метода ЗУП',
        'Остановим один вызов дважды, посмотрим стек и временную таблицу, '
        'затем изменим запрос только внутри выбранного вызова.',
        setup=OVERVIEW_SETUP,
        snapshot_note='Пример рассчитан на ЗУП КОРП 3.1.38.92 и платформу 8.5.1.1529; дата демоснимка — 01.08.2021. ',
        setup_note='\n\nУстановите пакет из PyPI по [корневому README](../../../README.md). Перед запуском измените '
                   '`PLATFORM_BIN`, `CONNECTION_STRING` и `SOURCE_ROOT` в следующей ячейке. '
                   'Если нужны учётные данные, задайте их локально в `RuntimeConfig`.')
    capture += [
        md('## Выбираем сотрудников\n\nБерём тех же работающих на дату сотрудников, '
           'что и в обзоре. Для `КадровыеДанныеСотрудников` нужен список ссылок.'),
        bsl(OVERVIEW_EMPLOYEES),
        bsl('СотрудникиДемо = СписокСотрудников.ВыгрузитьКолонку("Сотрудник");'),
    ]
    capture += capture_control()
    capture += [
        md('## Меняем результат одного вызова\n\nПовторно ставим те же точки. '
           'В останове A меняем текст локального `Запрос`: новый запрос читает прежнюю '
           'временную таблицу и умножает ФОТ на 1,1. Исходная конфигурация не меняется.'),
        py('''runtime.add_capture_point(MODULE_PATH, 127)
runtime.add_capture_point(MODULE_PATH, 129)'''),
        bsl(CAPTURE_CALL),
        py("experiment_a = runtime.status()\nassert experiment_a.state.value == 'captured'"),
        bsl('КонтекстОтладки.Запрос.Текст = "ВЫБРАТЬ Сотрудник, Организация, ФОТ * 1.1 КАК ФОТ ИЗ ВТКадровыеДанныеСотрудников";'),
        py('''experiment_b = runtime.resume_capture()
assert experiment_b.state.value == 'captured'
assert experiment_b.operation_id == experiment_a.operation_id'''),
        bsl(CAPTURE_OUTPUT),
        py('''from decimal import Decimal

changed_output = СнимокВыхода.to_df(refs='uuid')
expected = sort_rows(temporary).copy()
expected['ФОТ'] = expected['ФОТ'].map(lambda value: Decimal(str(value)) * Decimal('1.1'))
actual = sort_rows(changed_output)
pd.testing.assert_frame_equal(expected[keys], actual[keys])
assert [Decimal(str(value)) for value in expected['ФОТ']] == [
    Decimal(str(value)) for value in actual['ФОТ']]
display(changed_output)
print('ФОТ после изменения запроса:', changed_output['ФОТ'].sum(), '₽')'''),
        py('''experiment_done = runtime.resume_capture()
assert experiment_done.succeeded and experiment_done.state.value == 'completed'
assert experiment_done.operation_id == experiment_b.operation_id
runtime.clear_capture_points()'''),
        md('## Новый вызов снова использует типовой запрос\n\nИзменение локального объекта '
           'действовало только в остановленном вызове. Повторим операцию без точек.'),
        bsl(CAPTURE_CALL),
        bsl('ПланПовторКратко = ПланПовтор.Скопировать(, "Сотрудник,Организация,ФОТ");'),
        py('''restored = ПланПовторКратко.to_df(refs='uuid')
pd.testing.assert_frame_equal(sort_rows(temporary), sort_rows(restored))
display(restored)
print('ФОТ после обычного вызова:', restored['ФОТ'].sum(), '₽')'''),
        md('## Граница опыта\n\nМы изменили свойство локального объекта `Запрос`, '
           'а не код уже начатого вызова. Для присваивания нового значения скалярной '
           'локальной переменной нужен другой механизм.'),
    ] + cleanup()
    for folder, name, cells in [('ZUP', '01-overview', overview),
                                ('ZUP', '03-capture', capture),
                                ('UT', '05-ut-sales', ut_overview_cells())]:
        for index, cell in enumerate(cells):
            cell.id = f'{name}-{index:02d}'
        notebook = nbf.v4.new_notebook(
            cells=cells,
            metadata={
                'kernelspec': {'display_name': '1C BSL Demo', 'language': 'python', 'name': 'onec-demo'},
                'language_info': {'name': 'python'},
            },
        )
        nbf.validate(notebook)
        destination = DEMO_DEST / folder
        destination.mkdir(parents=True, exist_ok=True)
        nbf.write(notebook, destination / (name + '.ipynb'))
        print(folder, name, len(cells), 'cells')

if __name__ == '__main__': build()
